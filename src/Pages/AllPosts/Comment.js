import React from "react";

const Comment = ({ comment }) => {
    return (
        <div>
            <img src="" alt="" />
            <h1>{comment.comment}</h1>
        </div>
    );
};

export default Comment;
